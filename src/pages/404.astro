<main>
    <div class="image-container">
        <img src="/img/cat.jpg" alt="A cat on the 404 department" draggable="false">
        <div class="image-title">The cat that works on the 404 department</div>
        <div class="image-body">
            kindly informs you to
            <br>
            <a href="/">GO BACK TO MY PAGE.</a>
        </div>
    </div>
</main>

<style>
    @import "/styles/resets.css";
    @import "/styles/colors.css";
    @import "/styles/fonts.css";

    main {
        width: 100vw;
        height: 100vh;
        padding: 2rem;

        font-family: var(--display-font);
        font-weight: 400;
        font-feature-settings: "lnum";
        color-scheme: light dark;

        /* Font smoothing */
        font-synthesis: none;
        text-rendering: optimizeLegibility;
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
        -webkit-text-size-adjust: 100%;

        /* Non-selectable text */
        -webkit-touch-callout: none;
        -webkit-user-select: none;
        -khtml-user-select: none;
        -moz-user-select: none;
        -ms-user-select: none;
        user-select: none;
        cursor: default;

        /* Colors */
        color: var(--fg-color);
        background-color: var(--bg-color);
    }

    .image-container {
        display: block;
        margin-left: auto;
        margin-right: auto;
        width: 100vh;
        padding: 0.5rem;
        box-shadow: 2px 4px 8px var(--gray);
        border-radius: 0.25rem;
        border: 1px solid var(--gray);
        background-color: var(--bg-color);
    }

    .image-title {
        font-size: 3vw;
        padding-bottom: 0.5rem;
        font-weight: bold;
        text-align: center;
    }

    .image-body {
        font-size: 3vw;
        text-align: center;
    }

    img {
        width: 66vw;
    }

    a {
        /* text-decoration: underline; */
        animation: under_line 0.8s infinite;
    }

    @keyframes under_line {
        0% {
            text-decoration: none;
        }
        100% {
            text-decoration: underline;
        }
    }

    @media (prefers-color-scheme: light) {
        main {
            --fg-color: var(--night);
            --bg-color: var(--bone);
        }
    }
    @media (prefers-color-scheme: dark) {
        main {
            --fg-color: var(--smoke);
            --bg-color: var(--night);
        }
    }
</style>
